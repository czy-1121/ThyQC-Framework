# Anonymized examples

This directory contains a small set of four-frame contact sheets prepared for paper illustration and code smoke testing. They are anonymized examples, not the full internal test set.

The full internal test set is not included until its de-identification and redistribution status has been verified separately. The accompanying `case_catalog.csv` uses pseudonymous case codes and contains no direct identifiers.
